import Vue from 'vue';
// By default we import all the components.
// Only reserve the components on demand and remove the rest.
// Style is always required.
import { Slide } from 'cube-ui';
Vue.use(Slide);
//# sourceMappingURL=cube-ui.js.map